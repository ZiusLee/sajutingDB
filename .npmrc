# Prevent native bindings
node-gyp-binary=false
node-linker=hoisted
prefer-frozen-lockfile=false
# Skip optional dependencies that might require native compilation
ignore-scripts=true
# Handle peer dependencies more gracefully
legacy-peer-deps=true
